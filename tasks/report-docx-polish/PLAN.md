# PLAN: Полировка отчёта — подпись шкалы, цвет легенды, диаграммы

Дата: 2026-08-07

## Шаги

1. **Убрать подпись шкалы** (3 места): div в Report360View.tsx:263-265;
   para в reportDocx.ts:319; div в SubjectPanel.tsx:125. Больше нигде
   `scalePoints.map(...).join(' · ')` не используется — проверить grep'ом.
2. **Цвет плашек SCALE**: в helpers.ts к элементам SCALE добавить поле
   `fill` ('fbdcdc' / 'fdf3b3' / 'cdf2cd' — те же hex, что в scaleBg);
   в reportDocx.ts:314 рендерить label как TextRun с shading{fill} +
   пробел + desc обычным текстом. Веб-пилюли не трогаются (используют cls).
3. **Диаграммы парами**: chartParagraphs → chartLayout: при 2+ картинках —
   Table без границ, 2 колонки по ~4700 DXA (ширина A4-контента ~9350),
   в ячейке: подпись категории (bold, по центру) + ImageRun шириной ~300 px
   с сохранением пропорций; нечётная последняя — одна ячейка/по центру;
   одиночная картинка — как сейчас (620). Использовать во всех вызовах
   (общая диаграмма, пары самооценки, пары внешних групп).
4. **Проверки**: tsc/build frontend; ручной DOCX-смоук не автоматизирован
   (нужен браузер) — проверка человеком на живом отчёте после деплоя;
   структурная проверка — сборка без ошибок типов docx.
5. Коммит и пуш в mvp2; чек-лист приёмки в итоговом сообщении.

## Альтернативы

- Вставлять название категории внутрь SVG перед конвертацией (без таблицы):
  отвергнута — правит DOM на лету и дублирует уже существующую подпись
  карточки; таблица в DOCX проще и надёжнее позиционирует пары.
- Уменьшить картинки, оставив их друг под другом: отвергнута человеком
  при опросе (выбран вариант «рядом, как в PDF»).

## Риски

- 🟡 Читаемость подписей осей при ширине 300 px — радары рендерятся
  size=680 с крупным шрифтом, при уменьшении вдвое шрифт ~достаточен;
  проверить глазами на живой выгрузке, при нечитаемости — вернуться
  к варианту «друг под другом» (согласовать).
- 🟢 Изменения только фронтенд/выгрузка, бэкенд и схема не трогаются.

## Бюджет

Файлов: 4 (helpers.ts, Report360View.tsx, SubjectPanel.tsx, reportDocx.ts)
/ Время: ~40 мин.

## Чек-лист выхода

- [x] шаги конкретны (сделан/не сделан)
- [x] есть отвергнутая альтернатива с содержательной причиной
- [x] красных рисков нет
- [x] бюджет назначен
